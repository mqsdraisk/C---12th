// typecasting in c++:

#include <iostream>

using namespace std;

int main(){

    //implicit typecasting means when the compiler itself converts a specific datatype of a varibable to another: for example:

    int num1 = 3, num3;
    float num2;
    num3 = num1 + num2;
    cout<<num3; // the datatype will be a float

    //explicit typecasting means when the user himeself converts a specific datatype of a variable to another:

    

}